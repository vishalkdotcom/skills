# Code the ticket

`CODING_STANDARDS.md`. TDD at ticket seams (`tdd` skill). During non-trivial edits, run the `check` and `typecheck` scripts `package.json` names. Findings hard or judgement in the progress file: [../ship/review-rules.md](../ship/review-rules.md) — clear those, then re-run check and typecheck.

**Done when:** every acceptance criterion holds in the working tree. If Findings listed hard or judgement, those are cleared as well.

## Progress

Update the progress file from the template. `unit_done: implement`. `next: validate`. Pasteable next prompt naming `ship-validate`.

**Done when:** that file is on disk with evidence and next prompt. Then stop.
