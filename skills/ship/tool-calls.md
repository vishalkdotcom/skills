# Tool calls

On every `gh` call, pass `--repo owner/repo` from this repo's `origin` (`git remote get-url origin`).

When a tool returns a classification error or Cancelled: one retry (with approval if the environment offers it), then another tool or ask the human.
